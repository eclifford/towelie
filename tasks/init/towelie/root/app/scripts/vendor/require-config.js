require.config({
    baseUrl: "/",
    paths: {
      'jquery': 'scripts/vendor/require-jquery',
      'text'  : 'scripts/vendor/text',
      'json'  : 'scripts/vendor/json'
    },
    waitSeconds: 30
});